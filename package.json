{
  "name": "test-driver",
  "private": true,
  "version": "0.0.1",
  "description": "Test Driver",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve"
  },
  "dependencies": {
    "@ionic/core": "6.0.5"
  },
  "devDependencies": {
    "@stencil/core": "^2.13.0",
    "@stencil/router": "^1.0.1"
  }
}
